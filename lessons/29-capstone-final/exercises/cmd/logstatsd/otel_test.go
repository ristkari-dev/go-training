package main

import "testing"

// TestGRPCSpan is a SKELETON. Stand up the gRPC server over bufconn with
// the (traced) interceptors, make a call, and assert the interceptor
// recorded a span via an in-memory exporter. (See the solution for the
// bufconn + tracetest pattern. Requires the grpcsrv interceptors to be
// implemented first.)
func TestGRPCSpan(t *testing.T) {
	// TODO
}
